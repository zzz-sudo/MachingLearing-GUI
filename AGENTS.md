# 通用项目开发规则

作者: Kuroneko

本文件是一套可以直接复制到其他项目根目录的通用开发规则。规则不假定项目采用某一种界面、运行形态或编程语言。项目实际采用的技术栈、启动方式和部署方式必须由项目自身说明文件补充。

## 规则优先级

本文件约束代码写入、文件处理、测试和交付过程。项目的法律要求、用户明确要求、运行环境限制和安全要求优先于本文件。

发生冲突时按照以下顺序处理:

1. 用户当前明确提出的要求
2. 数据安全、密钥安全和法律合规要求
3. 项目现有接口、数据格式和兼容性承诺
4. 本文件的通用规则
5. 个人代码风格偏好

如果无法判断某一条要求，应先阅读项目现有文档、配置和测试。不要用猜测替代事实，也不要为了绕过冲突而删除现有功能。

## 项目开头信息

每个需要交付的项目都必须在根目录说明文件开头写明以下信息。说明文件可以是 README.md，也可以是项目约定的其他文档。

~~~text
项目输入文件格式:
项目输出文件类型:
项目功能:
作者: Kuroneko
~~~

输入文件格式必须列出真实支持的扩展名和内容限制。输出文件类型必须列出真实生成的文件、目录、接口响应或数据库记录。没有实现的格式不得写成已支持。

如果项目没有文件输入或文件输出，也必须明确写出无文件输入或无文件输出，并说明实际输入和输出是什么。

## 文字和编码

1. 源代码、配置文件、结构化数据、测试文件和项目文档统一优先使用 UTF-8。
2. 源代码默认使用 UTF-8 without BOM。只有目标工具明确要求 BOM 时才使用 BOM。
3. 新建文件不得依赖操作系统默认编码。读写文件时要显式指定编码。
4. 文本文件导入时按以下顺序识别 UTF-8 BOM、UTF-16 LE BOM、UTF-16 BE BOM、严格 UTF-8、GB18030 和项目明确声明的其他编码。
5. 编码识别结果不确定时，必须返回编码错误类型、文件名、尝试过的编码和失败位置，不得静默替换乱码字符。
6. 内部字符串统一转换为 Unicode。导出时明确指定 UTF-8、UTF-8 with BOM 或用户选择的目标编码。
7. CSV 导入必须处理 BOM、分隔符、引号、换行符、表头位置和空字段。不能只通过文件扩展名决定解析方式。
8. 文件名、目录名和接口参数中的非 ASCII 字符必须按原值保留。传输到 URL 时使用 URL encoding，不能手工拼接未经编码的路径。
9. 日志、错误信息和导出文件不得因为控制台或系统区域设置而改变编码。
10. 修复乱码时必须保留原始文件，并记录原始编码、目标编码和转换结果。不得直接覆盖唯一的原始文件。

## 注释和文档

1. 源代码中的短注释和行内注释统一使用 English comment，避免同一段注释混用中英文。
2. 复杂算法、数据流、外部接口和不直观的设计必须有详细中文说明，放在项目文档、模块说明或公共 API 文档中。
3. 注释解释为什么这样做、输入输出和限制条件，不重复描述显而易见的代码。
4. 注释必须与代码同步修改。过期注释比没有注释更危险。
5. 错误信息应包含可操作的事实，例如错误类型、文件名、字段名、行号、阶段和建议处理方式。
6. 文档中的示例命令、路径、环境变量和输出必须与当前代码一致。不能保留已经失效的占位示例。
7. 用户可见文字使用清晰的中文或项目规定的语言。禁止使用表情、装饰性符号、对号、叉号和没有实际含义的图案字符。

## 文件输入、输出和路径

1. 任何文件处理模块都必须先声明支持的输入格式、输出格式和不支持的格式。
2. 文件扩展名只用于初步路由。真正解析前应通过文件内容、魔数或结构化解析器确认格式。
3. 原始输入文件必须保留只读副本，并记录原始文件名、大小、修改时间、来源和内容哈希。
4. 生成文件应写入明确的输出目录，临时文件应写入独立临时目录。临时文件不能冒充最终产物。
5. 使用临时文件加原子替换时，只有写入完成且校验成功后才能替换目标文件。
6. 路径拼接必须使用标准路径 API。禁止通过字符串拼接绕过目录边界检查。
7. 项目内路径、用户路径和外部路径必须区分保存。显示名称不能当作真实路径使用。
8. 不得把完整大文件直接放入界面状态、日志或数据库单字段。应保存引用、分页数据、统计信息或流式结果。
9. 导出文件必须声明格式、编码、版本和来源。结构化输出应包含必要的 schema 或版本字段。

## 压缩文件和嵌套输入

1. 用户提供压缩文件时，必须先解压到项目隔离目录，再按解压后的真实格式继续处理。
2. 原始压缩包必须保留，解压结果必须记录 parent asset、压缩格式和解压时间。
3. 解压前必须拒绝路径穿越、绝对路径、符号链接或项目明确不允许的特殊文件。
4. 必须设置文件数量、单文件大小、总解压大小和嵌套层数上限。超过上限时返回明确的 ArchiveLimitError。
5. 压缩包中的文件名必须经过编码处理后再写入文件系统。无法可靠识别的文件名不得静默改名。
6. 解压后的文件要按照真实扩展名和内容进入对应解析流程。不能把压缩包整体当作某一种业务文件。
7. 解压失败时保留原始压缩包，清理不完整的临时输出，并返回错误类型、成员名称和失败原因。

## 模块设计和必要耦合

项目必须保持必要的耦合，而不是把每个模块写成无法协作的独立示例。模块之间通过稳定的领域对象、接口和事件协同，避免通过隐式全局变量共享状态。

推荐至少定义以下一组可追踪对象，名称可以按项目领域调整:

1. Project 表示一个工作范围或业务上下文。
2. Resource 表示原始文件、外部输入或导入对象。
3. Task 表示一次可追踪的处理、分析或计算任务。
4. Artifact 表示任务产生的文件、报告、模型或结构化结果。
5. Audit 表示输入、配置、版本、错误和输出之间的来源关系。

解析、转换、计算和导出模块必须围绕这些对象共享标识、版本和状态。每个模块都可以独立测试，但不得通过复制一套相互矛盾的数据模型来假装解耦。

模块边界必须满足以下要求:

1. 外部输入在边界处完成校验和规范化。
2. 核心业务只依赖稳定接口，不直接依赖界面组件、命令行输出或具体存储实现。
3. 长耗时任务通过 Task 状态和可恢复结果交互，不在调用方同步阻塞整个流程。
4. 每个 Artifact 都能追溯到输入 Resource、配置版本和生成 Task。
5. 更换存储、运行方式或界面时，核心数据契约和业务行为保持不变。

## 错误处理和防御性代码

1. 在文件、网络、数据库、外部进程和用户输入边界进行一次明确校验。
2. 通过稳定契约保证的内部条件，不重复添加无意义的空值判断、类型判断和默认分支。
3. 不得用静默跳过、空字符串、空数组或自动切换到另一种模式掩盖真实错误。
4. 错误必须有稳定的错误类型或错误代码，并包含人类可读的信息。
5. 错误信息不得包含密码、令牌、私钥、完整个人数据或不必要的原始内容。
6. 可恢复的错误应说明恢复动作，例如重新选择编码、确认字段类型或清理输出目录。
7. 不可恢复的错误应停止当前任务并保留足够的审计记录，不能返回看起来成功的结果。
8. 捕获异常时只在能够增加上下文或进行清理时重新抛出。禁止使用无差别捕获后继续执行。
9. 外部进程失败时记录退出码、阶段、标准错误摘要和使用的配置，不记录完整秘密环境变量。

错误信息建议使用以下结构:

~~~text
错误类型: FileEncodingError
错误信息: 无法使用声明的编码读取文件
文件: data/example.csv
阶段: import
建议: 选择正确编码后重试
~~~

## 配置、密钥和隐私

1. 默认配置必须安全、可解释并适合本地开发。生产配置不能写死在源码中。
2. 密钥、密码、令牌、证书私钥和真实用户文件不得提交到 Git。
3. 环境变量示例只能放占位值，并说明变量用途、格式和是否必填。
4. 读取配置时应区分缺失、格式错误和权限不足，返回不同错误类型。
5. 日志必须脱敏。调试日志也不得输出完整秘密、授权头、Cookie 或私有文件内容。
6. 需要上传数据时，必须说明上传目标、传输方式、保留时间和失败后的清理行为。
7. 测试数据应使用公开数据、合成数据或已获得授权的数据，不得把真实用户文件复制到仓库。

## 运行方式

规则不规定项目必须采用某一种界面或部署形态。每个项目只需要说明自己实际支持的运行方式。

运行说明必须包含:

1. 必需的运行时、版本和系统条件。
2. 依赖安装命令。
3. 开发运行命令。
4. 测试命令。
5. 构建或导出命令。
6. 必需环境变量及示例值。
7. 常见失败的错误类型和排查路径。

命令必须使用项目实际路径和当前脚本名称。不得为了看起来完整而加入项目未实现的启动方式。长耗时任务必须说明状态查询、日志位置、取消行为和结果位置。

## 测试和验证

每次功能修改都必须根据影响范围执行验证。至少覆盖以下内容:

1. 正常输入。
2. 空输入、缺失字段和错误字段类型。
3. 中文文件名、中文内容和 UTF-8、UTF-16、GB18030 等需要支持的编码。
4. 大文件、长文本和分页或流式处理。
5. 压缩包、嵌套目录和解压安全边界。
6. 外部依赖不可用、权限不足和中途失败。
7. 刷新、重启或重复执行后能否恢复已持久化结果。
8. 输出文件能否被标准工具重新读取。

测试结果必须记录执行的命令、通过数量、失败数量和仍然存在的限制。不能只说明测试通过而不说明测试范围。

## Git 工作流

每完成一个可验证的阶段，必须先检查差异，再创建一个目标单一的 Git 提交。提交前执行:

~~~powershell
git status --short
git diff --check
git diff --stat
~~~

提交信息使用清晰的英文或中英文混合短语，说明实际变化，不使用无意义的 update 或 change。提交后检查:

~~~powershell
git log -1 --oneline
git status --short
~~~

项目已配置远程仓库且用户要求同步时，将提交推送到当前分支。推送前确认远程地址、分支和提交内容。禁止使用强制推送覆盖他人提交，禁止使用 destructive reset 或 checkout 丢弃未确认的工作区修改。

临时文件、构建产物、缓存、私密配置、大型真实数据和本地运行目录必须加入 .gitignore。需要共享的测试数据必须明确来源、许可证和体积限制。

## 依赖和许可证

1. 新增依赖前说明用途、版本范围、许可证和替代方案。
2. 锁定文件必须与依赖声明同步提交。
3. 不得因为一个小功能引入体积很大的依赖，除非有可验证的收益。
4. 生产输出包含第三方代码、模型或数据时，必须保留来源、许可证和适用限制。
5. 不确定的许可证不得标记为已经审核，必须输出待人工确认的状态。

## AI 辅助开发规则

1. 写代码前先读取相关目录、接口、测试和配置，确认现有行为。
2. 修改前说明将改变的文件和行为范围，避免无关重构。
3. 优先复用项目已有模式、依赖和接口，不为单个需求创建重复抽象。
4. 代码写入后立即运行最小相关测试，再按风险扩大验证范围。
5. 发现用户已有修改时必须保留并基于现状工作，不能擅自覆盖或回退。
6. 未完成、未验证或被环境阻塞的内容必须明确说明，不得把计划写成已经实现。
7. 最终交付必须给出修改文件、验证命令、验证结果和已知限制。

## 交付前审查

交付前逐项确认:

1. 项目开头已经写明输入格式、输出类型、功能和作者。
2. 文件编码、中文文件名和导出编码经过验证。
3. 压缩文件按照规则解压并保留来源关系。
4. 错误类型和错误信息足够定位问题。
5. 模块之间使用统一数据契约，结果可以追溯。
6. 关键流程有真实测试，不能只有演示数据和假状态。
7. 密钥、用户文件、缓存和构建产物没有进入 Git。
8. 代码注释、文档、命令和实际实现一致。
9. Git 差异、测试结果和提交状态已经检查。
10. 最终说明没有夸大尚未实现的功能，也没有遗漏已知限制。
